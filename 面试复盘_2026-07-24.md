# 模拟面试复盘

> 2026-07-24 · 面试官视角 · 6 个核心问题 + 深度剖析

---

## 面试全景：这 6 个问题在考察什么？

面试官提的每一个问题背后都藏着一个考察维度。不要只看问题表面——看他想从你的回答里判断什么。

| 序号 | 问题类型 | 表面在问 | 实际在考察 |
|------|---------|---------|-----------|
| Q1 | 项目介绍 | "介绍一下你的项目" | **结构化表达能力** — 能不能在 2 分钟内让人听懂你在做什么 |
| Q2 | 设计决策 | "怎么缓解瓶颈" | **工程思维** — 遇到问题你是改代码还是改架构？ |
| Q3 | 技术理解 | "LLM 能区分 0.85 和 0.7 吗" | **对 LLM 本质的认知** — 你知道它是什么、不是什么 |
| Q4 | 边界漏洞 | "Agent 互相推诿怎么办" | **临场反应** — 被问到没考虑过的问题时会不会硬编 |
| Q5 | 并发/部署 | "关了浏览器任务会丢吗" | **系统设计** — 你是在写脚本还是在做系统 |
| Q6 | 深度反思 | "最难的地方在哪" | **自我认知** — 你能否跳出代码看到本质问题 |

---

## Q1: 项目介绍

**面试官**：用 2 分钟介绍一下你的项目。

### 你的原始回答（精简）

> "使用 LangGraph 分为四个节点……搜索节点通过 Tavily 搜索……分析节点进行置信度区分……撰写节点负责文档撰写……审核节点对报告进行审核打回……最大流转次数 3 次……搜索节点使用 Tavily 免费搜索，分析节点使用 dp-v4-flash……"

### 面试官在听什么

这不是在考你"知不知道四个节点的名字"——他看的是你的**思维方式**。一堆节点名字 = 一个功能清单 = 没设计思维。他要的是：**你做的这个系统解决了一个什么问题，你是怎么思考的。**

### 回答评价

**好的部分**：四个 Agent 的职责讲清楚了，MAX_ITERATIONS=3 + 质量声明的兜底策略被主动提到了——这是设计亮点。

**需要改进的**：结构散——从节点跳到模型又跳到搜索 API，没有主线。缺少"为什么这样设计"。大模型型号说太细反而暴露出没分类重点。遗漏了工程亮点（FastAPI、日志、LangSmith）——这些让"写了几个脚本"升级为"做了个系统"。

### 改进版回答（你们可以参考这个结构）

> 我做了一个多 Agent 的市场分析报告生成系统。用户输入一个分析主题，系统自动搜索互联网信息，然后四个 Agent（搜索、分析、撰写、审核）协作生成一份结构化的 Markdown 报告。
>
> 这里最核心的设计是**审核回退闭环**——审核节点发现质量问题后，小问题回撰写节点局部修改，大问题（推理链断裂）回分析节点重新推理。为了防止无限循环，最多迭代 3 轮，超限就强制输出但加上质量声明。我的想法是：一份不完美的报告比没有报告好。用户看到质量声明后可以人工复核。
>
> 工程层面，我给它加了 FastAPI 接口和 Gradio 前端，后台用异步任务执行，接了 LangSmith 做全链路追踪。

**为什么这个版本更好**：30 秒讲清楚做什么 → 点出一个核心设计决策 → 一句话带过工程配套。面试官听完有追问的点（"审核怎么判断大问题还是小问题？"），而不是迷失在细节里。

---

## Q2: 如何缓解搜索和分析节点的瓶颈

**面试官**：你说报告质量瓶颈在搜索和分析节点，那你做了什么来缓解？

### 你的原始回答（精简）

> "搜索节点暂没有直接缓解，更多放在分析节点上……对置信度进行明确规定，设立明确的分析条件放在系统提示词中……设定一定的重试次数……进行格式处理而不依赖模型的强制格式输出……各个使用 LLM 的节点通过 config 进行模型选择管理，使用共同的方法进行包装……"

### 面试官在听什么

这不是在考你"用了什么技术"——他看的是你**解决问题有没有层次感**。你能分清哪些问题用 Prompt 解决、哪些用代码解决、哪些用架构解决吗？

### 回答评价

**好的部分**：四件事（置信度锚点、重试、格式清洗、config 管理）都说到了，尤其是"格式处理不依赖模型的强制格式输出"这句话很到位——说明你知道 Prompt 的边界在哪。

**需要改进的**：四件事被平铺了，没有串成主线。面试官听完脑子里是四个碎片，不是一张图。

### 改进版回答

> 我在分析节点前后建了三层防线：
>
> - **Prompt 层**：置信度分级标准不是"请判断可信度"（太模糊），而是"来源是官方数据 + 有 2 个以上独立来源 = 0.9-1.0 区间"。把规则翻译成 LLM 能执行的 checklist。
> - **调用层**：温度递增重试（0 → 0.3 → 0.6），防止 DeepSeek 偶然波动导致空返回就报废整条管线。
> - **解析层**：不用 Prompt 强制 JSON 格式（LLM 做不到），而是写代码清洗 markdown 包裹 + 字段防御性取值。
>
> 另外所有 LLM 调用统一走 `call_llm_with_retry`，模型配置收归 config，换模型只改一行。

**为什么更好**："三层防线"给面试官一张清晰的图，"把规则翻译成 checklist"体现了对 Prompt 工程的深层理解。

---

## Q3: LLM 真的能区分 0.85 和 0.7 吗

**面试官**：LLM 本质是语言模型，不是评分器。你觉得它真能区分 0.85 和 0.7 吗？你怎么考虑的？

### 你的原始回答（精简）

> "我并不寄希望于 LLM 能给出明确的分数差距。我只需要它按照我的分级，根据来源是否权威、是否有其他机构反对，给出在某个分数区间的分数即可。这样后续撰写节点就能根据分数所在的区间选择不同的措辞。"

### 面试官在听什么

看你会不会**把 LLM 当成精算师**。如果回答"我的 Prompt 写得很清楚，置信度 0.83 和 0.84 的区别在于……"= 你对 LLM 的本质有误解。

### 回答评价

**很好**。你抓住了核心——不是让 LLM 当评分器，而是让它当分类器。而且你把置信度的**用途**讲出来了（传递给撰写节点影响措辞），说明这不是一个孤立的设计，而是跟下游有契约的。你的回答表明你对 LLM 能做什么、不能做什么有清醒的认知。

---

## Q4: Agent 互相推诿

**面试官**：审核节点打回分析节点时，分析拿到的还是同一批搜索数据。同一批数据重新推理，能得出不同结果吗？怎么保证重新分析不是浪费一轮？

### 你的原始回答（精简）

> "我考虑了重试时温度调整，但由于节点调用在外，每次返回分析节点时创建的 LLM 调用温度还是从 0 开始……不能保证重新分析后比第一次更好，它仍旧可能是无效分析。"

### 面试官在听什么

这是一个**压力测试**——故意问一个你设计上没有完全覆盖的边界情况，看你会不会硬编答案。

### 回答评价

**非常诚实**。你没有编答案，直接承认"是我考虑不周全了"。而且你主动分析了**为什么**温度递增不够——"因为节点调用在外，每次新建 LLM 调用温度归零"。这比给完美答案更有说服力。

### 改进方向（如果你想追问的话）

正确的改进方向不是改温度，而是**把审核的反馈传回给分析节点**——审核发现的问题（如"Finding A 的 evidence_ref 指向 source_2 但 source_2 没有该数据"）变成分析节点重新分析时的上下文：

> "请重新分析以下搜索结果。**特别注意：上一轮审核指出 Finding A 的证据不成立，请降置信度或删除此 Finding。**"

同一个数据 + 不同的上下文 = 不同的分析结果。温度递增解决的是"LLM 这轮运气不好"（恢复问题），审核反馈解决的是"同一个数据换个角度"（不同问题）。

---

## Q5: 并发与部署

**面试官（第一问）**：用户在等报告时关了浏览器，后台任务会丢吗？

### 你的回答（精简）

> "不会。我们使用异步后台任务，用户关闭浏览器不影响。但目前数据存在内存中，用户刷新后看不到已完成的报告，任务确实会在后台继续完成。"

### 评价

回答正确且诚实——指出了当前局限（内存存储），而不是捂着不说。

**面试官（第二问）**：两个用户同时提交主题会发生什么？

### 你的回答（精简）

> "当前会阻塞，因为线程池只有一个 worker。这个设计是为了防止 LangGraph 状态图被多个请求污染。但每次 build_workflow 成本很低，所以可以改 max_workers 来支持并发。"

### 评价

线程安全的考虑和解决方案都分析到位了，并且意识到了每次新建图就能解决并发污染问题。

**面试官（追问）**：多线程并发请求撞上 Tavily 和 DeepSeek 的 API 限流怎么办？

### 你的回答（精简）

> 分两种方案——多工具/多模型分流，或按节点重要性分配资源质量（搜索和分析用好的，撰写和审核用便宜的）。

### 评价

分层思维很好：战术层分流、战略层分级。但漏了最简单直接的——**指数退避**（429 时等 1s→2s→4s 再试，不是立即重试），这是代码里最近可以改进的。

### 面试官（扩展提问）：100 个用户上线怎么撑？

### 你的回答（精简）

> 前端给状态反馈避免重复提交 + 可变线程池。

### 评价

Java 线程池概念迁移得准确。但瓶颈不在线程管理——100 × 4 次 LLM 请求全打 DeepSeek，是 API 供应商扛不住。真正答案是**削峰**——上任务队列（Celery/Redis），前端提交的任务排队被消费，高峰期的任务在队列里等，不丢。

---

## Q6: 做 AI Agent 系统最难的地方在哪

**面试官**：不是问技术选型，问的是你真正踩过的坑。

### 你的原始回答（精简）

> "提示词的设定……得明确规定按什么标准去做。比如置信度判断，我必须明确写出'这条信息是不是来自官方？来自几个独立来源？'并给出对应区间。这是一个庞大工程，而不是像上司交给下属工作一样简单一句'你来划分一下置信度'。"
>
> "另外要给出明确的标准，并在代码层做好预处理和结果处理，做好代码防御，而不是完全信任 LLM。比如项目中经常遇到 DeepSeek 输出不可靠，就需要重试和结果处理。"

### 面试官在听什么

不是听你报技术栈，是想知道——**你从这两个项目里到底学到了什么**。最好的答案不是"Prompt 难写"，而是一个具体的、有画面感的洞见。

### 回答评价

**用得最好的部分**："不是像上司交给下属工作一样简单一句'你来划分一下置信度'"——这个比喻极好，面试官会记住这句话。比任何技术术语都更能证明你真正理解 LLM 的工作原理。

这句话背后的认知比任何技术答案都值钱——你理解了 Prompt 不是对话，是**规格说明书**。

---

## 六个问题背后的考核维度

| 面试官真正在判断什么 | 你的表现 |
|-------------------|---------|
| 结构化表达 — 能不能在 2 分钟内让人听懂你在做什么 | 内容对，但结构可以更主线化。建议先讲"做什么 → 最核心的设计决策 → 工程配套"，然后再展开细节 |
| 工程思维 — 遇到问题是改代码还是改架构 | 知道分层（Prompt / 调用 / 解析），这比只会写代码的高一档 |
| 对 LLM 本质的理解 — 知道它不是什么 | 很强。没把 LLM 当精算器，知道 Prompt 的边界在哪。这个认知在 junior 里不多见 |
| 临场诚实 — 遇到没考虑过的问题会不会编 | 不编，直接承认盲区，且能当场分析原因。面试官喜欢这种候选人 |
| 系统意识 — 写脚本还是做系统 | 线程安全、异步任务都想到了。限流和削峰是入职后的进阶内容 |
| 自我认知 — 你学到了什么 | 非常好。"上司交给下属"那个比喻会留在面试官脑子里 |

---

## 面试后你该记住的三句话

1. **面试官听到的不是知识点列表，是思维方式**——你能不能把一个复杂系统用 2 分钟讲清主线，比你会多少个技术术语重要得多。

2. **LLM 不是人，它是一台"按输入模板生成输出"的机器**。你的工作不是跟它对话，是**把模糊需求翻译成它可执行的客观约束条件**。理解了这一点，你就超过了 80% 的面向 AI Agent 岗位候选人。

3. **诚实 > 完美答案**。"我没考虑到这一点，但如果现在让我想……"比硬编一个答案强十倍。面试官不期待你什么都知道，他期待你不是一个不懂装懂的人。
